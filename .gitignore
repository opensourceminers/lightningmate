# dependencies
node_modules/

# build output
dist/
build/
*.tsbuildinfo

# env / secrets — never commit macaroons or certs
.env
.env.local
*.macaroon
tls.cert

# local runtime data (autopilot config, rebalance accounting, etc.)
.data/

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
.vscode/
.idea/

# Store gallery screenshots (contain node data — keep out of git)
screenshots/
